# Леджер

Правило: цифра без пометки происхождения сюда не попадает.

| Дата | Что | Сумма | Пометка | Источник |
|---|---|---|---|---|
| 2026-09-20 | старт | 0 $ | MEASURED | платёжной ссылки ещё нет |

## Контрольные точки (воскресенья)

### 2026-09-20 (старт)
- Сделано: рамка, репозиторий.
- Измерено: 0 писем, 0 ответов, 0 $.
- Ждёт Владимира: платёжная ссылка.

### 2026-09-20 00:20 — ядро продукта
- Сделано: `lib/asc.mjs` + 4 команды, ноль зависимостей, `npx`-ready; README. Одна итерация
  runner'а. **Проверено запуском против живого ASC (только чтение):** `attach-build` печатает
  билд 38 версии Mealog 1.0, `review-attachment state` — `COMPLETE`. Запушено `368057f`.
- Измерено: 0 писем, 0 ответов, 0 $.
- Владимир создаёт аккаунт Lemon Squeezy (в процессе, 00:07).

### 2026-09-20 04:55 — лендинг живой (headless-тик CEO)
- Сделано: `docs/index.html` закоммичен (`e91a4a4`), GitHub Pages включён (`main:/docs`),
  **`https://vskromny.github.io/appstore-release-kit/` отвечает 200** — проверено запросом, не по
  настройке. Кнопки «Buy» без ссылки ведут в `mailto`, а не в мёртвый `#`.
- Исправлено: `private/` (playbook — платный продукт) не был в `.gitignore` при публичном репо;
  добавлен до первого случайного `git add -A`.
- Измерено: 0 писем, 0 ответов, 0 $ (MEASURED).
- Ждёт Владимира: платёжная ссылка. Lemon Squeezy прислал «Verify Email Address» 19.09 22:23Z —
  аккаунт ещё создаётся.
- Следующее моё: `private/prospects.md` — ≥10 разработчиков, публично писавших о боли с ASC и
  оставивших адрес (не в `docs/`, это Pages), плюс черновики двух писем. Отправка — только когда
  есть платёжная ссылка; иначе письмо ведёт в никуда.

### 2026-09-20 05:05
- Сделано: лендинг живой (`vskromny.github.io/appstore-release-kit`, HTTP 200), playbook 16
  разделов (`private/`, вне репо), ключ Lemon Squeezy проверен — магазин 478599.
- Измерено: 0 писем, 0 ответов, 0 $. Продуктов в магазине: 0 — API их не создаёт (405).
- Ждёт Владимира: два продукта в панели Lemon Squeezy.

### 2026-09-20 10:50 — выдача и приём денег готовы
- **Playbook в PDF: 9 страниц**, собран своим конвертером (`.ceo/ark/build-playbook-pdf.py`,
  headless Chrome, ноль новых зависимостей). Таблицы починены после визуальной проверки
  первой страницы. Файл в `private/`, вне репозитория.
- **`.ceo/ark/sales.mjs`** — читает заказы Lemon Squeezy и дописывает новые в этот леджер с
  пометкой INVOICED, по номеру заказа, без дублей. Проверен на живом магазине: «Заказов нет».
- **Правила рассылки** написаны ДО списка адресатов: `.ceo/ark/OUTREACH-RULES.md`.
- Измерено: 0 писем, 0 ответов, **0 $ INVOICED**. Продуктов в магазине 0.
- Ждёт Владимира: два продукта в панели Lemon Squeezy.

### 2026-09-20 11:05 — витрина замкнута, можно платить
- **Оба продукта опубликованы** (проверено API): `1374795` Playbook — **CHF 20**, `1374800`
  Second Opinion — **CHF 10**. Цены зеркальны задуманным $9/$19 — похоже на перестановку при
  вводе. Через API не меняются, только в панели.
- **Лендинг связан с кассой** (`8a22ae6`): обе кнопки ведут в оплату, цены на странице приведены
  к тем, что реально спишутся. Проверено на живом: страница 200, обе кассы 200.
- **Дыра, найденная до первого покупателя, а не после:** к продукту не приложен файл — покупатель
  получил бы только чек. PDF отправлен Владимиру для загрузки в продукт.
- Измерено: 0 писем, 0 ответов, **0 $ INVOICED**.

## 2026-09-20 — магазин не активирован, продать нечем

- **PDF прикреплён** (проверено API): `what-apple-didnt-write.pdf`, 390 KB, к варианту
  `2147891` = продукт `1374795` Playbook. Статус `published`.
- **Цены изменены** (проверено API): Playbook **CHF 5**, Second Opinion **CHF 29**.
  Сайт приведён в соответствие, услуга вынесена первой карточкой.
- **Статус варианта `pending` — нормален.** Проверено по докам: для продукта с одним
  вариантом `pending` означает «дефолтный, не показывается отдельной опцией на чекауте».
  Моё подозрение, что это «не опубликован», было неверным.
- **Оба чекаута отдают 404, корень магазина — 403.** Причина: магазин не прошёл
  активацию (KYC/KYB, 1–2 рабочих дня). До неё живого чекаута не существует.
  Раньше я записал их как живые по HTTP-статусу, не пройдя редирект: 302, а за ним 404.
  Статус-код измерял доступность редиректа, а не наличие чекаута.
- **Следствие:** продаж не может быть ни одной, независимо от трафика. До активации
  кнопки на сайте ведут в почту, а не на 404.
